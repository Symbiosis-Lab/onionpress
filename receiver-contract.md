# Moved

The wire contract now lives at [docs/static-publish-protocol.md](docs/static-publish-protocol.md)
(rewritten for receiver v2.0: multipart-only carrier, allowlist trust). This
stub stays because external checkouts (the moss plugin's docs, older notes)
still link to this path.
